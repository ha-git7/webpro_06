<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>数当てゲーム</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            text-align: center;
        }
        h1 {
            color: #4CAF50;
        }
        .result {
            font-size: 1.5em;
            margin-top: 20px;
        }
        .attempts {
            margin-top: 10px;
            font-size: 1.2em;
        }
    </style>
</head>
<body>
    <h1>数当てゲーム</h1>

    <% if (result) { %>
        <!-- 結果の表示 -->
        <div class="result">
            <strong><%= result %></strong>
        </div>
    <% } else { %>
        <p>1から100の間で数を予想してください！</p>
    <% } %>

    <!-- ゲームフォーム -->
    <form action="/number-guess" method="get">
        <label for="guess">あなたの予想：</label>
        <input type="number" name="guess" id="guess" required min="1" max="100" />
        <input type="submit" value="挑戦！">

        <!-- 隠しフィールドで試行回数を送信 -->
        <input type="hidden" name="attempts" value="<%= attempts %>">
    </form>

    <% if (attempts > 0) { %>
        <!-- 試行回数の表示 -->
        <div class="attempts">
            <p>試行回数: <%= attempts %></p>
        </div>
    <% } %>

    <!-- 正解の数の表示（デバッグ用）-->
    <p>正解の数: <%= correctNumber %></p>
</body>
</html>